# ========== 核心框架 ==========
streamlit>=1.56.0

# ========== 3D可视化核心修复 ==========
ipython_genutils==0.2.0
py3Dmol==2.0.4

# ========== 数据处理 ==========
pandas>=1.5.0
numpy<2,>=1.19.3
scikit-learn>=1.3.0
umap-learn==0.5.5
joblib>=1.3.2

# ========== 化学信息学 ==========
rdkit-pypi==2022.9.5
chembl_webresource_client>=0.10.9

# ========== PyTorch 生态 ==========
# --extra-index-url 必须放在需要它的包之前（uv 兼容）
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.2+cpu
torchvision==0.16.2+cpu
torchaudio==2.1.2+cpu

# PyTorch Geometric（从官方 PyPI 获取，其 setup.py 会自动拉 torch-scatter 等依赖）
torch-geometric==2.4.0

# ========== 其他依赖 ==========
matplotlib>=3.7.0
pillow>=10.0.0
plotly>=5.18.0
seaborn>=0.12.0
setuptools>=68.0.0
rich>=13.0.0
markdown-it-py>=2.2.0

# ========== 蛋白-配体相互作用分析 ==========
# plip 依赖 openbabel Python 绑定（需从源码编译 SWIG），Streamlit Cloud 无法构建
# 本地开发请手动安装: pip install plip
# plip>=2.3.0
nglview>=3.1.0
ipywidgets>=8.0.0

# ========== 激酶结合模式相似性 (KLIFS REST API) ==========
requests>=2.31.0

# ========== 分子对接 ==========
# openbabel Python 绑定同 plip 一样需 SWIG 编译，云端不可用
# 本地开发请手动安装: pip install openbabel
# openbabel>=3.1.0

# ========== 分子动力学模拟 (OpenMM) ==========
# ⚠️ 仅限本地 conda 环境使用，Streamlit Cloud 无法安装
# 本地安装: conda install -c conda-forge openmm openmmforcefields openff-toolkit pdbfixer mdtraj
# openmm>=8.0
# openmmforcefields>=0.12
# openff-toolkit>=0.15
# pdbfixer>=1.9
# mdtraj>=1.9

# ========== MM-GBSA 结合自由能 (OpenMM + MDTraj) ==========
# ⚠️ 仅限本地 conda 环境使用，Streamlit Cloud 无法安装
# 本地安装: conda install -c conda-forge openmm mdtraj numpy pandas
# openmm>=8.0
# mdtraj>=1.9
