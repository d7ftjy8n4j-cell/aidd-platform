# ========== 核心框架 ==========
streamlit>=1.56.0

# ========== 3D可视化核心修复 ==========
ipython_genutils==0.2.0
stmol==0.0.9
py3Dmol==2.0.4

# ========== 数据处理 ==========
pandas>=1.5.0
numpy<2,>=1.19.3
scikit-learn>=1.3.0
umap-learn==0.5.5
joblib>=1.3.2

# ========== 化学信息学 ==========
rdkit-pypi==2022.9.5
chembl_webresource_client>=0.10.9

# ========== PyTorch 生态 ==========
# --extra-index-url 必须放在需要它的包之前（uv 兼容）
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.2+cpu
torchvision==0.16.2+cpu
torchaudio==2.1.2+cpu

# PyTorch Geometric（从官方 PyPI 获取，其 setup.py 会自动拉 torch-scatter 等依赖）
torch-geometric==2.4.0

# ========== 其他依赖 ==========
matplotlib>=3.7.0
pillow>=10.0.0
plotly>=5.18.0
setuptools>=68.0.0
rich>=13.0.0
markdown-it-py>=2.2.0
