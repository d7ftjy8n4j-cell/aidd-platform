# 统一使用 LF 换行符，避免 Windows/Unix 混用造成假 diff
* text=auto eol=lf

# 二进制文件不做行尾转换
*.pkl binary
*.pth binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary

# Windows 批处理必须是 CRLF（cmd.exe 对 LF-only 的 if/for 块不保证按行解析）
*.bat text eol=crlf
